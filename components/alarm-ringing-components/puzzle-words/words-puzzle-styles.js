import { StyleSheet } from "react-native";

export const wordsPuzzlePageStyles = StyleSheet.create({
    taskDescription: {
        marginTop: 60,
        marginBottom: 10,
        fontSize: 24,
        fontFamily: 'lato-medium'
    },
    scrollContainer: {
        flexGrow: 1,
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    }
})