import { StyleSheet } from "react-native";

export const alarmSettingsStyles = StyleSheet.create({
    container: {
        marginTop: 30,
        width: "95%",
        backgroundColor: "rgba(255, 255, 255, 0.4)",
        borderRadius: 25,
        display: "flex",
        flexDirection: "column",
    },
    scrollContainer: {
        alignItems: "center",
        justifyContent: "center"
    },
    optionContainer: {
        marginTop: 10,
        marginBottom: 10
    }
})