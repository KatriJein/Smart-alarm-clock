import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    scrollView: {
      marginTop: 15,
      width: "90%",
      marginBottom: 15
    },
    pageTitle: {
      marginTop: 15,
      fontSize: 38
    }
  })