import { StyleSheet } from "react-native";

export const pushAnswerStyles = StyleSheet.create({
    button: {
        width: 45,
        height: 45,
        borderRadius: 999,
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: 'rgba(202, 202, 202, 1)',
        marginRight: 5
    }
})